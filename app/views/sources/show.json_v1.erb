[{count:<%=@object_values.length%>},{version:1}<%if @total_count and @total_count > 0 and @object_values.length > 0%>,{total_count:<%=@total_count%>}<%if @token%>,{token:"<%=@token%>"},<%end%><%end%><%wlist_size = 0%><%@wrapped_list.each do |source_name,oblist|%>{s:"<%=source_name%>",ol:[<%ob_size = 0%><%oblist.each do |objkey,obj|%>{o:"<%=objkey%>",av:[<%obj.each_with_index do |o,i|%><%if o[:db_operation] == "delete"%>{i:<%=o[:id]%>}<%else%>{a:"<%=o[:attrib]%>",i:<%=o[:id]%>,v:"<%=o[:value]%>"}<%end%><%=i < obj.length-1 ? "," : "]}"%><%end%><%=ob_size < oblist.length-1 ? "," : nil%><%ob_size += 1%><%end%>]}<%=wlist_size < @wrapped_list.length-1 ? "," : nil%><%wlist_size += 1%><%end%>]